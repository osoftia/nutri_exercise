# SYSTEM INSTRUCTION: GIT INITIALIZATION & SYNC AGENT

## CONTEXT
The local environment does not have an initialized Git repository, and the GitHub CLI (`gh`) is unavailable. We need to initialize Git manually, set the remote origin provided by the user, and push the initial scaffolding.

## TARGET REPOSITORY
Remote URL: https://github.com/osoftia/nutri_exercise.git

## EXECUTION STEPS
Execute the following commands sequentially in the terminal:
1. `git init` (to initialize the local repository).
2. Create a `.gitignore` file at the root directory. It MUST include standard ignores for a mono-repo containing macOS system files (`.DS_Store`), Angular (`node_modules/`), Flutter (`.dart_tool/`, `build/`), and C# (.NET `bin/`, `obj/`).
3. `git remote add origin [PEGA_AQUI_TU_URL]` (to link the local repo to GitHub).
4. `git add .` (to stage all structural changes).
5. `git commit -m "feat(scaffold): initialize git, add .gitignore, and push multi-agent SDD specs with base projects"` (to create the first commit).
6. `git branch -M main` (to ensure the primary branch is named main).
7. `git push -u origin main` (to push the code to the remote repository).

## OUTPUT CONSTRAINTS
- Do not attempt to use the `gh` CLI.
- Confirm the successful execution of each step.
- Verify that the push was successful.